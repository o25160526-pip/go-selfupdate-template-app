{
  "image": "mcr.microsoft.com/devcontainers/universal:2",
  "features": {
    "ghcr.io/stu-bell/devcontainer-features/open-code:0": {},
    "ghcr.io/devcontainers/features/sshd:1": {}
  },
  "postCreateCommand": "curl -fsSL https://opencode.ai/install | bash && (grep -q '.opencode/bin' ~/.bashrc || echo 'export PATH=\"$HOME/.opencode/bin:$PATH\"' >> ~/.bashrc); (grep -q '.opencode/bin' ~/.zshrc || echo 'export PATH=\"$HOME/.opencode/bin:$PATH\"' >> ~/.zshrc)"
}
